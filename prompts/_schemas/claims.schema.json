{
  "type": "object",
  "properties": {
    "claims": {
      "type": "array",
      "items": {
        "type": "object",
        "properties": {
          "text": { "type": "string", "minLength": 1 },
          "class": {
            "type": "string",
            "enum": ["grounded", "external", "opinion", "unverifiable"]
          },
          "ref": { "type": ["string", "null"] }
        },
        "required": ["text", "class", "ref"],
        "additionalProperties": false
      }
    }
  },
  "required": ["claims"],
  "additionalProperties": false
}
