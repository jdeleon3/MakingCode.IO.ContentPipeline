"""Trend sweep (TDD 12 WP-16): `hn.py`/`rss.py` are the two signal sources,
`scan.py` matches them against `config.sweep.topics` and scores recurrence
across the last few sweeps. See `scan.py`'s module docstring for the shape
of a run.
"""
