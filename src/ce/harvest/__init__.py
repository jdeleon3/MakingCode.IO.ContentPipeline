"""Harvest stage (TDD 10.3, 10.4, 10.5-adjacent): turns a finished project's
git history, transcripts, and external research into `data/projects/<slug>/
harvest/*` artifacts. Orchestrated by `ce harvest` (WP-08).
"""
